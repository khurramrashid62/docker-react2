sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t khurramrashid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true khurramrashid/docker-react npm run test
